let myArray = [2, 'Pete', 2.99, 'another string'];
myArray[0];
myArray[2];
myArray[myArray.length - 1];

let array = [1, 2, 3];
array[1] = 4;

array;

array[array.length] = 10;

array;

array.push('a');

array;

array.push(null, 'xyz');

array;