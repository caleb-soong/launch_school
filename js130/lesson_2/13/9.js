/* eslint-disable id-length */
const arr = [1, 2, 3];
const obj = { a: 'a', b: 'b', c: 'c' };

const [ one, two, three ] = arr;
const { a, b, c } = obj;

console.log(one, two, three);
console.log(a, b, c);